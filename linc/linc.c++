#include <linc/linc.h++>

auto willCollide(TriangleMesh const &mesh, Pivots const &pivots,
                 float layerHeight) -> bool {
  (void)pivots;
  (void)mesh;
  (void)layerHeight;
  return false;
}
